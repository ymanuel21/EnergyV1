# Session Notes — 30-07-2026

## Critical fixes applied

### 1. ProductPickerField brand crash (React objects error)
- **File**: src/components/admin/ProductPickerField.tsx:87
- **Root cause**: API returns `brand: { name: 'EcoFlow' }` (object), component rendered `{p.brand}` directly
- **Fix**: `{typeof p.brand === 'string' ? p.brand : p.brand?.name || ''}`
- **Symptom**: Delayed crash at t≈10s — "This page couldn't load"

### 2. ProductPickerField placeholder collision with SearchBar
- **Fix**: Single-mode placeholder changed from `"Cari produk..."` to `"Pilih produk unggulan..."`
- **Impact**: Selector `input[placeholder="Cari produk..."]` was matching header SearchBar instead of CMS picker

### 3. Chip remove button collision with section close
- **Fix**: Remove button text from `×` to `"Hapus"` to avoid matching section editor's close button

### 4. Dropdown click propagation
- **Fix**: Added `e.stopPropagation()` on dropdown container + each item

### 5. Publish infinite loading
- **File**: src/app/admin/homepage/HomepageBuilder.tsx:98
- **Fix**: Wrapped `await onSave(fd)` in try/catch to clear loading state on error

### 6. DB connection pool exhaustion (4 leaks)
- **Files**: admin/settings/page.tsx, api/asset/[id]/route.ts, api/badges/route.ts, api/upload/route.ts
- **Fix**: Replaced per-request `new Pool()` + `new PrismaClient()` with shared singletons `getPrisma()` / `getAdminPrisma()`
- **Impact**: 4 routes × 5 conns = 20 wasted connections per cold start eliminated

### 7. Preview fallback
- **File**: src/lib/api/homepage-sections.ts
- **Fix**: Preview mode fetches `status IN ('draft','published')`, prefers draft, falls back to published
- **Before**: Preview always showed grid when no drafts existed

### 8. HomepageBuilder EditorField product-picker case
- **File**: src/app/admin/homepage/HomepageBuilder.tsx:414
- **Fix**: Added `case 'product-picker'` to EditorField switch (was falling to default plain text input)
- **Import**: `import { ProductPickerField } from '@/components/admin/ProductPickerField'`

## Playwright auth lessons
- Login form is **index 1** on admin/login page (search form is index 0)
- Correct selector: `form[action="/api/login"] button[type="submit"]`
- Session cookie: `__Secure-authjs.session-token`

## DB diagnostic rules
- NEVER use inline `node -e` for DB queries — creates connections
- ALWAYS use file-based scripts with `pool.end()` for cleanup
- Singleton pools: `lib/db.ts` (getPrisma) + `admin/lib/admin-prisma.ts` (getAdminPrisma)
- Neon max connections: 5 per pool

## Homepage CMS editor architecture
- TWO editing UIs: SectionEditor.tsx (orphaned, never imported) + HomepageBuilder's internal EditorField
- HomepageBuilder EditorField is the real editor — renders at line 362
- ProductPickerField must be imported in HomepageBuilder, not SectionEditor
- SectionEditor.tsx changes have NO effect on the running app

## Vercel deployment
- 100/day limit — batch changes, deploy only on explicit request
- 5+ file rewrites → `rm -rf .next` before rebuild

## Current state (end of session)
- Admin homepage: stable, no delayed crash
- ProductPicker: autocomplete works, unique placeholder, safe brand rendering
- Publish flow: loading state properly cleared
- DB pool: per-request leaks fixed
- Preview: matches public homepage
